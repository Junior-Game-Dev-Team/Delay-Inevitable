using UnityEngine;

[CreateAssetMenu(fileName = "New Context", menuName = "Game Flow/New Context")]
public class ContextStateSO : ScriptableObject
{
    // Essentially to be used as an extensible enum substitute.
}
